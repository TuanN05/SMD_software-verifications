# --- Server Config ---
server.port=8080

# --- PostgreSQL Configuration ---
# Docker maps 5432 to 5433, see docker-compose.yml
spring.datasource.url=jdbc:postgresql://localhost:5433/smd_db
spring.datasource.username=root
spring.datasource.password=rootpassword
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Config (Hibernate tự động nhận diện Dialect, nhưng có thể cấu hình rõ ràng nếu cần)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# DDL Auto: update để tự động tạo bảng
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Redis Configuration ---
spring.data.redis.host=localhost
spring.data.redis.port=6379

# --- Elasticsearch Configuration ---
spring.elasticsearch.uris=http://localhost:9200
# --- JWT Configuration ---
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000

# --- File Upload Configuration ---
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload.path=uploads/syllabus/pdf

# --- Monitoring Configuration (Grafana & Prometheus) ---
# Kích hoạt các endpoints giám sát
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=always

# Gán nhãn cho ứng dụng (để sau này phân biệt nếu bạn chạy nhiều app)
management.metrics.tags.application=smd-core-service

# Thêm dòng này nếu chưa có
ai-service.url=http://localhost:8000